{
  "framework": "nextjs",
  "buildCommand": "node scripts/switch-to-postgres.js && prisma generate && next build",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "env": {
    "PRISMA_GENERATE_NO_ENGINE": "1"
  }
}
