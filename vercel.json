{
  "framework": "nextjs",
  "buildCommand": "node scripts/switch-to-postgres.js && prisma generate && npx prisma db push --accept-data-loss && npm run db:seed && next build",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "env": {
    "PRISMA_GENERATE_NO_ENGINE": "1"
  },
  "crons": [
    {
      "path": "/api/cron/ping",
      "schedule": "0 */12 * * *"
    },
    {
      "path": "/api/cron/scheduler",
      "schedule": "0 * * * *"
    }
  ]
}
